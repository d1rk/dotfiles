#!/bin/bash
file=/home/deploy/.ssh/authorized_keys
key=`echo $@ | cut -f1- -d\ `
if sudo grep -Fxq "$key" $file
then
	echo "Key already exists in $file"
else
	echo "$key" | sudo tee -a $file
	echo "Key added to $file"
fi